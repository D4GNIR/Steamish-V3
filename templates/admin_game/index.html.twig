{% extends 'base-admin.html.twig' %}

{% block title %}Hello AdminGameController!{% endblock %}

{% block body %}
<div class="container">
    <div class=''>
        {# <a href="{{ path('///') }}" class="btn btn-success"><i class="fa-solid fa-circle-plus"></i></a> #}
    </div>
    <table class="table table-hover text-light table-dark">
        <tr class="row">
            <td class="col-lg-3"><b>Nom</b></td>
            <td class="col-lg-3"><b>Prix</b></td>
            <td class="col-lg-3"><b>Date de parution</b></td>
            <td class="col-lg-3"><b>Action</b></td>
        </tr>
        {% for game in pagination %}
        <tr class="row">
            <td class="col-lg-3">
                <p class="link">
                    {{ game.name }}
                </p>
            </td>
            <td class="col-lg-3">
                <p class="link">
                    {{ game.price }} €
                </p>
            </td>
            <td class="col-lg-3">
                <p class="link">
                    {{ game.publishedAt|date("d/m/Y") }}
                </p>
            </td>
            <td class="col-lg-3">
                <a class="btn btn-primary" href="{{ path('app_game_show', {'slug': game.slug}) }}">
                    <i class="fa-solid fa-eye"></i>
                </a>
                <a class="btn btn-steamish" href="">
                    <i class="fa-solid fa-pen-to-square"></i>
                </a>
                <a class="btn btn-danger" href="">
                    <i class="fa-solid fa-trash-can"></i>
                </a>
            </td>
        </tr>
        {% endfor %}
    </table>
    <div>
        {{ knp_pagination_render(pagination) }}
    </div>
</div>

{% endblock %}