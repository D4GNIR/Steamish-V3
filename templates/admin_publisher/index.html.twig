{% extends 'base-admin.html.twig' %}

{% block title %}Hello AdminPublisherController!{% endblock %}

{% block body %}
<div class="container">
    <div class=''>
        <a href="{{ path('app_admin_publisher_create') }}" class="btn btn-success"><i class="fa-solid fa-circle-plus"></i></a>
    </div>
    <table class="table table-hover text-light table-dark">
        <tr class="row">
            <td class="col-lg-3"><b>Nom</b></td>
            <td class="col-lg-3"><b>Lire</b></td>
            <td class="col-lg-3"><b>Editer</b></td>
            <td class="col-lg-3"><b>Supprimer</b></td>
        </tr>
    {% for publisher in publishers %}
        <tr class="row">
            <td class="col-lg-3">
                <p class="link">
                    {{ publisher.name }}
                </p>
            </td>
            <td class="col-lg-3">
                <a class="btn btn-primary" href="{{ path('app_admin_publisher_show', {'slug': publisher.slug}) }}">
                    <i class="fa-solid fa-eye"></i>
                </a>
            </td>
            <td class="col-lg-3">
                <a class="btn btn-steamish" href="{{ path('app_admin_publisher_edit', {'slug': publisher.slug}) }}">
                    <i class="fa-solid fa-pen-to-square"></i>
                </a>
            </td>
            <td class="col-lg-3">
                <a class="btn btn-danger" href="{{ path('app_admin_publisher_delete', {'slug': publisher.slug}) }}">
                    <i class="fa-solid fa-trash-can"></i>
                </a>
            </td>
         </tr>
    {% endfor %}
</div>
{% endblock %}
