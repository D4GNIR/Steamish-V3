<nav class="navbar navbar-expand-lg navbar-dark common bg-darko fixed-top">
  <div class="container-fluid">
    <a class="logo" href="{{ path('app_home') }}">
      <img src="/build/images/logo.png" class="img-fluid" width="100" height="100" alt="logo">
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <div class="row mx-auto">
        <div class="col-12">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0 d-flex justify-content-around">
            <li class="nav-item">
              <a class="link Splus m-4" href="#sorties">Les dernières sorties</a>
            </li>
            <li class="nav-item">
              <a class="link Splus m-4" href="#most">Les jeux les plus joués</a>
            </li>
            <li class="nav-item">
              <a class="link Splus m-4" href="#best">Les meilleures ventes</a>
            </li>
          </ul>
          <ul class="navbar-nav me-auto mb-2 mb-lg-0 d-flex justify-content-center">
            <li class="">
              <a class="link Splus m-5" href="{{ path('games') }}">Tous les jeux</a>
            </li>
            <li class="nav-item">
              <a class="link Splus m-5" href="{{ path('app_publisher') }}">Les éditeurs partenaires</a>
            </li>
          </ul>
        </div>
      </div>
        <div class='col-3'>
            {{ 
              render(controller('App\\Controller\\HomeController::searchBar')) 
            }}
          <div>
          </div>
          <div>
            {% if app.user %}
              <a href="{{ path('app_logout') }}" class="btn btn-steamish mt-3"><i class="fa-solid fa-user-xmark"></i></a>
              <a href="{{ path('accountSlug', {'slug': app.user.slug}) }}" class="btn btn-steamish mt-3"><i class="fa-solid fa-user-gear"></i></a>
                {% if is_granted("ROLE_ADMIN") %}
                    <a href="{{ path('app_admin') }}" class="btn btn-steamish mt-3"><i class="fa-solid fa-chart-pie"></i></a>
                {% endif %}
            {% else %}
              <a href="{{ path('app_register') }}" class="btn btn-steamish mt-3">Inscription</a>
              <a href="{{ path('app_login') }}" class="btn btn-steamish mt-3"><i class="fa-solid fa-user-check"></i></a>
             
            {% endif %}    
              <a href="{{ path('app_forum') }}" class="btn btn-steamish mt-3"><i class="fa-solid fa-khanda"></i></a>        
          </div>
        </div>
    </div>
  </div>
</nav>